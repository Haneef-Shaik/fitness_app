# Expo/React Native autolinking and Gradle resolve native modules from a flat
# node_modules. apps/mobile/.npmrc says so too, but a root `pnpm install`
# reads only this file; without it the release APK build cannot find modules.
node-linker=hoisted
