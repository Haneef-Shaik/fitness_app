# Prelude for an installed RELEASE build (`com.volt.app`): sign-in.yaml with the
# app chosen for it. One sign-in, so the two cannot drift — they had, by 108
# lines, before G10 folded them together.
appId: com.volt.app
name: Sign in (release build)
env:
  EMAIL: demo@volt.app
  PASSWORD: voltdemo1234
---
- runFlow:
    file: sign-in.yaml
    env:
      EMAIL: ${EMAIL}
      PASSWORD: ${PASSWORD}
      APP_ID: com.volt.app
