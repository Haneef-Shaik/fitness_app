"""Signing in is Supabase Auth's job; this package is the API's side of it
(docs/14-SUPABASE.md, S1–S6): verifying Supabase's access tokens, checking the
sign-in is still live, creating the FitLog account on first use, and the
server-only calls to Supabase's Auth API."""
